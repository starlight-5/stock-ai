# -*- coding: utf-8 -*-
"""
V1 SMC 전략의 핵심 파라미터(상수)를 한곳에 모아둔 모듈입니다.

이 파일을 수정하면 전략 전체에 적용되므로,
파라미터 튜닝 시 여기만 수정하면 됩니다.
실제 운영 시에는 config.yaml에서 덮어쓰도록 설계할 수 있습니다.

[2026-05-11 개선 내역 - US 시장 최적화]
  - TP1 RR 4.0 → 2.0 : 현실적인 1차 익절 목표
  - TP2 RR 6.0 → 4.0 : 너무 먼 목표 제거
  - TP1_CLOSE_RATIO 0.0 → 0.5 : 1차 익절 시 50% 분할 청산 활성화
  - FVG_ATR_MULTIPLIER 0.15 → 0.5 : 노이즈성 소형 FVG 필터링 강화
  - AI_PROB_THRESHOLD 0.4 → 0.55 : AI 필터 신뢰도 상향
"""

# ─────────────────────────────────────────────────
# [리스크 관리 파라미터]
# ─────────────────────────────────────────────────

# 일일 최대 허용 손실율 (-5% 이하가 되면 킬 스위치 발동)
DAILY_DRAWDOWN_LIMIT: float = -0.05

# 1회 매매 시 총 자본 대비 리스크 비율 (0.01 = 1%)
TRADE_RISK_RATIO: float = 0.01

# ─────────────────────────────────────────────────
# [익절/손절 파라미터]
# ─────────────────────────────────────────────────

# 1차 TP의 리스크-리워드(RR) 배율
# [개선] 4.0 → 2.0 : 미국 1분봉 단위에서 RR 4배는 너무 멀어 TP에 닿기 전에 되돌림 발생.
#         2.0배로 현실화하여 TP1 도달 빈도를 높이고 분할 익절을 유도함.
TP1_RR_RATIO: float = 2.0

# 2차 TP의 고정 RR 배율 (나머지 50% 물량을 들고 가는 최종 목표)
# [개선] 6.0 → 4.0 : TP2도 현실적인 수준으로 조정
TP2_RR_RATIO_FALLBACK: float = 4.0

# 왕복 수수료율 (0.25% × 2 = 0.5%)
COMMISSION_RATE: float = 0.005

# 1차 TP 체결 시 청산할 비율
# [개선] 0.0 → 0.5 : 50% 분할 익절 활성화.
#         TP1 도달 시 절반 수익 확보 + 나머지 SL을 본절(Breakeven)로 이동
TP1_CLOSE_RATIO: float = 0.5

# ─────────────────────────────────────────────────
# [차트 분석 파라미터]
# ─────────────────────────────────────────────────

# POI(관심 구역) 탐지에 사용할 캔들 개수 (현재 5분봉 기준)
POI_CANDLE_COUNT: int = 120

# 진입 확정 신호 분석에 사용할 캔들 개수 (현재 1분봉 기준)
ENTRY_CANDLE_COUNT: int = 20

# FVG 탐지: ATR 기반 동적 최소 갭 크기
# ATR(평균 실제 범위)을 기준으로 갭 크기의 유효성을 판단합니다.
# 변동성이 높을 때는 기준이 커지고, 낮을 때는 작아지는 동적 필터입니다.

# ATR 계산 기간 (캔들 수)
ATR_PERIOD: int = 14

# 갭 크기가 ATR × 이 배수 이상이어야 유효한 FVG로 인정
# [개선] 0.15 → 0.5 : 미국 1분봉 기준 0.15는 너무 낮아 노이즈 갭도 진입 조건으로 인식.
#         0.5(ATR의 50% 이상)로 상향하여 진짜 기관 주문에 의한 Imbalance만 인식하도록 함.
FVG_ATR_MULTIPLIER: float = 0.5

# ATR을 계산할 수 없을 때(캔들 부족 등) 사용하는 고정 비율 fallback
# 현재가 대비 비율 (0.1% = 0.001)
FVG_MIN_SIZE_RATIO: float = 0.003  # [개선] 0.0005 → 0.003 (진입 기준 상향)

# OB 도지캔들 인정 기준
# 몸통(|close-open|)이 전체 캔들 범위(high-low) 대비 이 비율 이하이면 도지로 간주
# 예: 0.3 → 몸통이 전체 범위의 30% 이하면 도지 캔들
OB_DOJI_BODY_RATIO: float = 0.4

# 꼬리(위 또는 아래) 길이가 몸통 대비 이 배수 이상이어야 '꼬리가 긴 도지'로 인정
# 예: 2.0 → 꼬리 길이 ≥ 몸통 길이 × 2배
OB_DOJI_WICK_RATIO: float = 2.0

# ─────────────────────────────────────────────────
# [타이밍 파라미터]
# ─────────────────────────────────────────────────

# 2.5단계 SL/TP 재계산 주기 (초) - 진입 타임프레임(1분봉) 마감에 맞춰 재계산
STANDBY_RECALC_INTERVAL_SEC: int = 60  # 1분

# 킬 스위치 감시 주기 (초)
KILL_SWITCH_CHECK_INTERVAL_SEC: int = 60  # 1분

# ─────────────────────────────────────────────────
# [킬 존(Kill Zone) 시간대 필터]
# ─────────────────────────────────────────────────

# 미국 동부 기준 주요 킬 존 (UTC 기준)
# [개선] 신규 추가: 개장 직후 1시간(14:30~15:30 UTC)은 유동성 헌팅이 극심.
#         이 시간 동안은 SWEEP & REVERSE를 노리기 어려운 일반 FVG 진입을 제한.
#         백테스트에서는 이 시간 진입을 스킵하여 휩소 손절 빈도 감소 기대.
# UTC 기준 미국 개장 킬존: 14:30~15:30 (한국시간 23:30~00:30)
KILL_ZONE_START_UTC_HOUR: int = 14  # UTC 14시 (미국 개장)
KILL_ZONE_END_UTC_HOUR: int = 15    # UTC 15시 (개장 후 첫 1시간 제한)
KILL_ZONE_MINUTE_END: int = 30      # 15:30 UTC까지 제한

# ─────────────────────────────────────────────────
# [기본 거래 설정]
# ─────────────────────────────────────────────────

# 기본 거래소 코드 (NAS = 나스닥)
DEFAULT_EXCD: str = "NAS"

# 기본 거래 종목 코드 (config.yaml에서 덮어씀)
DEFAULT_SYMBOL: str = "AAPL"

# 주문 유형 코드: "00" = 지정가, "01" = 시장가
ORDER_TYPE_MARKET: str = "00"   # KIS API에서 해외주식 시장가 코드
ORDER_TYPE_LIMIT: str = "00"    # 해외주식 지정가

# 매수/매도 방향 코드 (KIS API 규격)
BUY_DVSN: str = "02"   # 매수
SELL_DVSN: str = "01"  # 매도

# ─────────────────────────────────────────────────
# [AI 하이브리드 필터 파라미터]
# ─────────────────────────────────────────────────

# AI가 판단한 성공 확률이 이 값 이상일 때만 진입 승인
# [개선] 0.4 → 0.55 : OOS 결과에서 AI가 낮은 신뢰도 신호를 통과시킨 사례가 많았음.
#         임계치를 높여 AI 방어력을 강화함.
AI_PROB_THRESHOLD: float = 0.55